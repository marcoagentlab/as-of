Canonical v0 seeds live in `src/lib/rwa/seed/` (TypeScript, validated on load).

- `assets.ts` — 10 coverage ids, honest nulls, `warnings: seed_stub`
- `sofr.ts` — `sofr_apy: null`, `quality: unavailable`

Do not invent token addresses or verified APYs here.
